# {{ title }}

**Generated:** {{ metadata.timestamp }}
**Config Hash:** {{ metadata.config_hash }}

---

## Overview

- **Evaluation Type:** {{ overview.eval_type }}
- **Variants Tested:** {{ overview.variant_count }}
- **Scenarios:** {{ overview.scenario_count }}

---

## Summary

{% if summary %}
| Variant | Avg Score | Total Score | Scenarios |
|---------|-----------|-------------|-----------|
{% for variant in summary -%}
| {{ variant.variant_id }} | {{ "%.2f"|format(variant.avg_score) }} | {{ "%.2f"|format(variant.total_score) }} | {{ variant.scenario_count }} |
{% endfor %}
{% else %}
No summary data available.
{% endif %}

---

## Detailed Results

{% if results %}
{% for result in results %}
### Scenario: {{ result.scenario_id }}

**Input:**
```json
{{ result.input | tojson(indent=2) }}
```

{% if result.variant_outputs %}
| Variant | Output | Scores |
|---------|--------|--------|
{% for output in result.variant_outputs -%}
| {{ output.variant_id }} | {{ output.output }} | {% for judge in output.judge_results %}**{{ judge.judge_id }}:** {{ judge.score }}/10{% if judge.reasoning %} ({{ judge.reasoning }}){% endif %}{% if not loop.last %}<br>{% endif %}{% endfor %} |
{% endfor %}
{% endif %}

---
{% endfor %}
{% else %}
No detailed results available.
{% endif %}

## Execution Metrics

- **Total Duration:** {{ telemetry.total_duration_seconds }}s
{% if telemetry.llm_calls -%}
- **LLM Calls:** {{ telemetry.llm_calls.total | default(0) }}
{% if telemetry.llm_calls.tokens -%}
- **Tokens:** {{ telemetry.llm_calls.tokens.prompt_total | default(0) }} prompt, {{ telemetry.llm_calls.tokens.completion_total | default(0) }} completion
{% endif %}
{% endif %}

---

*Report generated by gavel-ai*
